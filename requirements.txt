# requirements.txt — shared; each team adds its dependencies.

# LLM and agents (Team B/C)
langchain
langchain-openai
langchain-community
langgraph
openai

# MCP (Team A)
mcp

# Vector DB (Team A)
chromadb

# Hybrid retrieval — BM25 keyword search (Team A). Reranking uses sentence-transformers
# (CrossEncoder), already listed below.
rank-bm25

# Embeddings (Team A) — OpenAI embeddings used via Chroma's embedding function;
# sentence-transformers retained per project structure for optional local fallback.
sentence-transformers

# Document parsing + structure-aware chunking (Team A) — Docling converts documents
# (PDF/DOCX/PPTX/MD/HTML/images) into a structured DoclingDocument; HybridChunker chunks them
# with heading-aware context. Replaces pymupdf4llm + python-pptx.
docling

# Free, local, no-API-key OCR engine, used via Docling's RapidOcrOptions for scanned/handwritten
# PDFs and images (ONNX, no torch, best-effort on handwriting). Replaces tesseract/vision OCR.
rapidocr-onnxruntime

# Tokenizer alignment for HybridChunker (OpenAI text-embedding-3) + audio-transcript splitting.
tiktoken
langchain-text-splitters

# pymupdf retained only to build small PDF fixtures in tests; product PDF parsing is via Docling.
pymupdf

# Image handling for the OCR/imaging paths.
pillow

# Groq retained for lecture-audio transcription (ASR) only.
groq

# Lecture audio/video transcription (Team A) — used only when transcribing
faster-whisper
imageio-ffmpeg

# UI (Team C)
streamlit

# Schemas and validation (shared)
pydantic

# Environment (shared)
python-dotenv

# Testing (shared)
pytest
