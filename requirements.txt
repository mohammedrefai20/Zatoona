# requirements.txt — shared; each team adds its dependencies.

# LLM and agents (Team B/C)
langchain
langchain-openai
langchain-community
langgraph
openai

# MCP (Team A)
mcp

# Vector DB (Team A)
chromadb

# Hybrid retrieval — BM25 keyword search (Team A). Reranking uses sentence-transformers
# (CrossEncoder), already listed below.
rank-bm25

# Embeddings (Team A) — OpenAI embeddings used via Chroma's embedding function;
# sentence-transformers retained per project structure for optional local fallback.
sentence-transformers

# Document parsing + structure-aware chunking (Team A)
docling

# OCR for scanned PDFs / images (Team A)
rapidocr-onnxruntime

# Tokenizer + token-based splitting (Team A)
tiktoken
langchain-text-splitters

# PDF text-layer check + test fixtures (Team A)
pymupdf

# Image handling (Team A)
pillow

# Lecture audio/video transcription (Team A)
groq
faster-whisper
imageio-ffmpeg

# UI (Team C)
streamlit

# Schemas and validation (shared)
pydantic

# Environment (shared)
python-dotenv

# Testing (shared)
pytest
